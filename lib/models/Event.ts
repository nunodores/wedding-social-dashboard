export enum EventStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  COMPLETED = 'completed',
}

// // Re-export from database.ts for backward compatibility
// export { Event } from '../database';